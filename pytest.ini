[pytest]
testpaths =
    tests
    quant_system/tests
    quant_web/tests
    scripts/tests
norecursedirs =
    .git
    .venv
    data_warehouse
    private_recovery
    recovered_*
    generated
    vendor
addopts = --import-mode=importlib
markers =
    integration: integration checks that may start local services
